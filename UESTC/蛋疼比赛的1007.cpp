#include <iostream>
#include <string>
#include <cstring>
#include <cstdio>
using namespace std;

struct point
{
    int x,y;
}p[100];

int main()
{
    int n,k;
    while (scanf("%d%d",&n,&k) != EOF)
    {
        for (int i = 1;i <= n;i++)
            scanf("%d%d",)
    }
}

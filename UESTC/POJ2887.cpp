#include <iostream>
#include <string.h>
using namespace std;

char st[1000000],order,ich,s[1000][3000];
int n,fn,p,ls[1000];

void init
{
     int len;
     len = strlen(st);
     
int main()
{
    gets(st);
    init;
    cin >> n;
    for (fn = 1;fn <= n;fn++)
    {
        cin >> order;
        if (order == 'Q')
        {
                  cin >> p;
                  
        }
        if (order == 'I')
        {
                  cin >> ich >> p;
                  
        }
    }
    return 0;
}
                  

#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;

int n,t[60],c[60],tim;
double pos[60];
int fan[60];

int main()
{
    while (true)
    {
        scanf("%d",&n);
        if (n == 0) break;
        for (int i = 1;i <= n;i++)  scanf("%d%d",&t[i],&c[i]);
        for (int i = 1;i <= n;i++)
        {
            fan[i] = 0;
            pos[i] = 0.0;
        }
        for (int tim = 0;;tim++)
        {

        }
    }
}

#include <vector>
#include <string>
#include <iostream>
using namespace std;

class BinaryCode
{
      public:
      vector <string> decode(string message)
      {
             vector <string> result;
             string A;
             bool flag;
             int i,temp;
             int len;
             len = message.length();
             for (A[0] = '0';A[0] <= '1';A[0]++)
             {
                 flag = true;
                 for (i = 0;i < len;i++)
                 {
                     if (A[i-2]-'
                 }
             }
             return result;
      };
};

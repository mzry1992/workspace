#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;

int g,t,a,d;

int main()
{
    while (true)
    {
        scanf("%d%d%d%d",&g,&t,&a,&d);
        if (g == -1 && t == -1 && a == -1 && d == -1)   break;
    }
    return 0;
}

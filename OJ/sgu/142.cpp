#include <iostream>
#include <cstdio>
#include <cstring>
#include <algorithm>
using namespace std;

int len;
char s[500001];

int main()
{
    scanf("%d",&len);
    scanf("%s",s);
    for (int i = 0;i < len;i++)
    {

    }
	return 0;
}

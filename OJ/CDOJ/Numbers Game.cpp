#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;

int b,n,a[7],res;

int main()
{
    while (true)
    {
        scanf("%d%d",&b,&n);
        if (b == 0 && n == 0) break;
        res = 0;
        Gao(0,0);
    }
    return 0;
}

#include <iostream>
#include <cstdio>
using namespace std;

int n,m;
bool hash[100000][5][5];
int ans;

void DFS(int x,int y)
{
    if ((x == n) && (y == m))
    {

    }
}

int main()
{
    while (scanf("%d%d",&n,&m) != EOF)
    {
        ans = 0;
        DFS(1,1);
    }
}

#include <iostream>
#include <cstdio>
using namespace std;

int n,k,a[200];
int dp[200][200];

int main()
{
    int i,j;
    while (scanf("%d%d",&n,&k) == 2)
    {
          for (i = 1;i <= n;i++)
              scanf("%d",&a[i]);
          memset(dp,0,sizeof(dp));
          
    }    
}

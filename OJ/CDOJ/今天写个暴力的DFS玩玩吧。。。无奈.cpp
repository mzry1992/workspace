#include <cstdio>
#include <cstring>
#include <iostream>
using namespace std;

int n,m;
char map[12][12];

int main()
{
    while (true)
    {
        scanf("%d%d",&n,&m);
        if (n == 0 && m == 0)
            break;
        for (int i = )
    }
}

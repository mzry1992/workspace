#include <cstdio>
#include <cstring>
#include <string>
#include <iostream>
#include <vector>
using namespace std;

char s[101][120];
int n;
vector<string> res;
string tmp;

int main()
{
    while (scanf("%d",&n) != EOF)
    {
        res.clear();
        for (int i = 0;i < n;i++)
            scanf("%s",s[i]);
        for (int i = 0;i < )
    }
}

#include <iostream>
#include <cstdio>
#include <cstring>
#include <cstdlib>
using namespace std;

int main()
{
    int t;
    scanf("%d",&t);
    char map[10][10];
    for (int ft = 1;ft <= t;ft++)
    {
        int cw,cb;
        cw = cb = 0;
        gets(map[8]);
        for (int i = 0;i < 7;i++)
            gets(map[i]);
    }
}

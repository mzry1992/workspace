#include <iostream>
#include <cstdio>
#include <cstring>
using namespace std;

char s[100001],fb[3];
int n,len;

int main()
{
    scanf("%s",s);
    len = strlen(s);
    scanf("%d",&n);
    for (int i = 0;i < n;i++)
    {
        scanf("%s",fb);
    }
    return 0;
}
